# include this file by include.path
[user]
	name = xecua
	email = courser4848@gmail.com
	signingkey = 335095B0
[core]
	editor = nvim
[init]
	defaultBranch = main
[color]
	ui = auto
[alias]
	cl = clone
	a = add
	p = push
	po = push origin
	pl = pull --prune
	plo = pull origin
	f = fetch --prune
	m = merge
	mas = !git show-ref --quiet refs/heads/master && git checkout master || git checkout main
	s = status
	ss = status -sb
	c = commit -am
	co = checkout
	b = branch
	d = diff
	l = log
	g = log --graph --date=short --decorate=short --oneline
	r = rebase
	ri = rebase -i
	amend = commit -a --amend
	st = stash
	stl = stash list
	sta = stash apply
	stp = stash pop
	std = stash drop
	cf = cat-file -p
[commit]
	gpgsign = true
[rerere]
	enabled = true
[pull]
	rebase = false
[include]
	path = ~/.config/git/config.local
